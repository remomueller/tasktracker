<% @title = 'Boards' %>
<div class="page-header">
  <h1>
    <% if @project %>
      <%= link_to @project.name, @project %> &middot;
    <% end %>
    <%= @title %>
    <%= link_to "Create Board", (@project ? new_board_path(board: { project_id: @project.id }) : new_board_path), class: 'btn btn-xs btn-primary' %>
  </h1>
</div>

<%= form_tag boards_path, method: :get, remote: true, id: "boards_search", class: 'form-inline', data: { object: 'form-load' } do %>
  <%= hidden_field_tag :order, params[:order] %>
  <%= hidden_field_tag :project_id, params[:project_id] %>
  <%= text_field_tag 'search', params[:search], class: 'form-control' %>
  <%= submit_tag 'Search', class: 'btn btn-primary' %>
<% end %>

<div id="boards"><div class="center"><%= image_tag "contour/ajax-loader.gif" %></div></div>
