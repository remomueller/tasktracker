<% if @sticky %>
  <% @comments = @sticky.comments.page(params[:page]).per(params[:per]) %>
  $("#sticky_<%= @sticky.id %>_comments").html("<%= escape_javascript(render("comments/index")) %>");
<% end %>

resetSubmitButtons();
