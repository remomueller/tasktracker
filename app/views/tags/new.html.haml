- @title = 'New Tag'

.page-header
  %h1
    - if @tag.project
      = link_to @tag.project.name, @tag.project
      &middot;
    = link_to 'Tags', tags_path(project_id: @tag.project_id)
    &middot;
    New Tag

= render 'form'
