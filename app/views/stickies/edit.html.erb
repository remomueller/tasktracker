<% @title = "Edit Task #{@sticky.name}" %>

<div class="page-header">
  <h1>
    <% if @sticky.project %>
      <%= link_to @sticky.project.name, @sticky.project %> &middot;
    <% end %>
    <%= link_to "Task #{@sticky.name}", @sticky %> &middot; Edit
  </h1>
</div>

<%= render 'form' %>


