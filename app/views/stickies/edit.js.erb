$("#sticky-modal-new").find('.modal-content').html("<%= escape_javascript(render partial: "stickies/edit_modal") %>");
$("#sticky-modal-new").modal('show');
$("#sticky_description").focus();

typeaheadReady();
datepickerReady();
