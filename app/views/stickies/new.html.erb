<% @title = "New Task" %>

<div class="page-header">
  <h1>
    <% if @sticky.project %>
      <%= link_to @sticky.project.name, @sticky.project %> &middot;
    <% end %>
    New Task
  </h1>
</div>

<%= render 'form' %>
