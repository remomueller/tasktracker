<% @stickies.each do |sticky| %>
  $("#sticky_<%= sticky.id %>_row").removeClass('sticky<%= "-not" if sticky.completed? %>-completed');
  $("#sticky_<%= sticky.id %>_row").addClass('sticky<%= "-not" unless sticky.completed? %>-completed');
<% end %>
