    <%= sticky.name %> <%= "#{SITE_URL}/stickies/#{sticky.id}" %> <%= strip_tags(sticky.full_description).truncate(16) %><% if sticky.owner %>    Assigned To: <%= sticky.owner.name %><% end %>    <% unless sticky.due_date.blank? %>Due Date: <%= simple_date_and_weekday(sticky.due_date) %><% end %>    <% if sticky.completed? %> completed <% end %>    Tags: <%= sticky.tags.collect{|t| t.name}.join(', ') %>
