    <%= sticky.name %> <%= "#{ENV['website_url']}/stickies/#{sticky.id}" %> <%= strip_tags(simple_markdown sticky.full_description).truncate(16) %><% if sticky.owner %>    Assigned To: <%= sticky.owner.nickname %><% end %>    <% unless sticky.due_date.blank? %>Due Date: <%= simple_date_and_weekday(sticky.due_date) %><% end %>    <% if sticky.completed? %> completed <% end %>    Tags: <%= sticky.tags.collect{|t| t.name}.join(', ') %>
