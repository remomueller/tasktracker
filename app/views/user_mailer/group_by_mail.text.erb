<%= @group.user.name %> added Group <%= @group.name %> <%= "#{SITE_URL}/groups/#{@group.id}" %> with <%= pluralize @group.stickies.size, 'Task' %><% if @group.template %> from Template <%= @group.template.name %><% end %>.

<% unless @group.description.blank? %>
Group Description

  <%= @group.description %>
<% end %>

<%= @stickies = @group.stickies; render partial: 'sticky_list' %>

<%= @footer_txt %>
