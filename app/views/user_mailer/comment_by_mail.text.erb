<%= @comment.user.name %> COMMENTED on Sticky <%= @sticky.name %> located at <%= "#{SITE_URL}/stickies/#{@sticky.id}" %>.


<% @sticky.comments.limit(5).each do |comment| %>
<%= comment.user.name %> (<%= comment.created_at.strftime("on %b %d, %Y at %I:%M %p") %>):

      <%= comment.description %>


<% end %>


<%= render partial: 'sticky_note' %>

<%= @footer_txt %>
