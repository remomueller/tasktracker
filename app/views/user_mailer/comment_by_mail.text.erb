<%= @comment.user.name %> COMMENTED on Task <%= @sticky.name %> located at <%= "#{ENV['website_url']}/stickies/#{@sticky.id}" %>.


<% @sticky.comments.limit(5).each do |comment| %>
<%= comment.user.name %> (<%= comment.created_at.strftime("on %b %d, %Y at %I:%M %p") %>):

      <%= comment.description %>


<% end %>


<%= render 'sticky_note' %>
