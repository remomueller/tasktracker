%b
  = succeed ',' do
    Dear
    = @project_user.user.first_name
%br
%br
= @project_user.creator.name
%span{ style: 'padding:5px;background-color:#5cb85c;color:white' } added
you to
= succeed '.' do
  = link_to @project_user.project.name, "#{ENV['website_url']}/projects/#{@project_user.project.id}", style: link_style
%br
%br
- if @project_user.allow_editing?
  You may edit the project and add new tasks and comments to it!
- else
  You may view the project and add comments to it!

