%p{ style: p_style }
  Dear
  = succeed ',' do
    = @project_user.user.first_name

%p{ style: p_style }
  = @project_user.creator.name
  %span{ style: emphasis_style }
    added you as
    - if @project_user.allow_editing?
      an editor
    - else
      a viewer
  on
  = succeed '.' do
    = link_to @project_user.project.name, "#{ENV['website_url']}/projects/#{@project_user.project.to_param}", style: link_style

  - if @project_user.allow_editing?
    You may edit the project and add new tasks and comments to it!
  - else
    You may view the project and add comments to it!
