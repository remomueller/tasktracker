<%= "Assigned to: #{@sticky.owner.name}" if @sticky.owner %>

<%= "Due Date: #{simple_date_and_weekday(@sticky.due_date)}" unless @sticky.due_date.blank? %>
<%= "          #{@sticky.due_at_range}" unless @sticky.all_day? %>

<%= @sticky.full_description %>

<%= "Tags: #{@sticky.tags.collect{|t| t.name}.join(', ')}" if @sticky.tags.size > 0 %>
