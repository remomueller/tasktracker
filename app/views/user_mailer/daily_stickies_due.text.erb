Hello <%= @recipient.first_name %>,

<% if @recipient.all_deliverable_stickies_due_today.size > 0 %>
You have <%= pluralize @recipient.all_deliverable_stickies_due_today.size, 'sticky' %> due today. <%= "#{SITE_URL}/stickies?due_after=#{Date.today.strftime("%m%%2f%d%%2f%Y")}&due_before=#{Date.today.strftime("%m%%2f%d%%2f%Y")}&status%5B%5D=planned&owner_id=#{@recipient.id}" %>

<%= @stickies = @recipient.all_deliverable_stickies_due_today; render partial: 'sticky_list' %>

<% end %>
<% if @recipient.all_deliverable_stickies_past_due.size > 0 %>
You have <%= pluralize @recipient.all_deliverable_stickies_past_due.size, 'sticky' %> past due. <%= "#{SITE_URL}/stickies?due_before=#{(Date.today - 1.day).strftime("%m%%2f%d%%2f%Y")}&status%5B%5D=planned&owner_id=#{@recipient.id}&order=stickies.due_date+DESC" %>

<%= @stickies = @recipient.all_deliverable_stickies_past_due; render partial: 'sticky_list' %>

<% end %>
<% if @recipient.all_deliverable_stickies_due_upcoming.size > 0 %>
You have <%= pluralize @recipient.all_deliverable_stickies_due_upcoming.size, 'sticky' %> upcoming. <%= "#{SITE_URL}/stickies?due_after=#{(Date.today + 1.day).strftime("%m%%2f%d%%2f%Y")}&due_before=#{(Date.today.friday? ? Date.today + 3.days : Date.tomorrow).strftime("%m%%2f%d%%2f%Y")}&status%5B%5D=planned&owner_id=#{@recipient.id}&order=stickies.due_date" %>

<%= @stickies = @recipient.all_deliverable_stickies_due_upcoming; render partial: 'sticky_list' %>

<% end %>
View stickies on a calendar here: <%= "#{SITE_URL}/stickies/calendar" %>

Thank you!

<%= DEFAULT_APP_NAME %> System Mailer

<%= @footer_txt %>
