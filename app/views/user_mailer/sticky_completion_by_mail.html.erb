<%= render partial: 'email_header' %>

<%= @sender.name %> <span style="padding:5px;background-color:#428bca;color:white">completed</span> the following Task <%= link_to @sticky.name, "#{SITE_URL}/stickies/#{@sticky.id}", style: "color: #428bca" %> on Project <%= link_to @sticky.project.name, "#{SITE_URL}/projects/#{@sticky.project.id}", style: "color: #428bca" %>.<br />

<%= render partial: 'sticky_note' %>

<% if @sticky.group %>
  This task is part of Group <%= link_to @sticky.group.name, "#{SITE_URL}/groups/#{@sticky.group.id}", style: "color: #428bca" %><% if @sticky.group.template %> from Template <strong><%= @sticky.group.template.name %></strong><% end %>.<br />
  <%= @stickies = @sticky.group.stickies; render partial: 'sticky_list' %>
  <br />
<% end %>

<%= render partial: 'email_footer' %>
