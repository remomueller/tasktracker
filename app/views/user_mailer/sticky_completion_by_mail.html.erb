Hello <%= @recipient.first_name %>,<br />
<br />
<%= @sticky.owner.name %> <span style="border:1px solid #366DC2;background-color:#c2daff">COMPLETED</span> the following Sticky <%= link_to @sticky.name, "#{SITE_URL}/stickies/#{@sticky.id}" %> on Project <%= link_to @sticky.project.name, "#{SITE_URL}/projects/#{@sticky.project.id}" %>.<br />

<%= render partial: 'sticky_note' %>

<% if @sticky.group %>
  This sticky is part of Group <%= link_to @sticky.group.name, "#{SITE_URL}/groups/#{@sticky.group.id}" %><% if @sticky.group.template %> from Template <strong><%= @sticky.group.template.name %></strong><% end %>.<br />
  <%= @stickies = @sticky.group.stickies; render partial: 'sticky_list' %>
  <br />
<% end %>
<br />
Thank you!<br />
<br />
<%= DEFAULT_APP_NAME %> Mailer<br />
<br />
<%= @footer_html %>
