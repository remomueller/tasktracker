<% @title = "User: #{@user.name}" %>
<h1><%= @title %></h1>

<% if current_user.system_admin? %>
  <%= link_to image_tag('icons/edit.png', :alt => '') + "Edit User", edit_user_path(@user), :class => 'button' %>
  <%= link_to image_tag('icons/delete.png', :alt => '') + 'Delete User', @user, :confirm => "Are you sure you want to delete User #{@user.name}?", :method => :delete, :class => 'button negative' unless current_user == @user %>
<% end %>
<%= link_to image_tag('icons/mydocuments.png', :alt => '') + "View All Users", users_path, :class => 'button' %>

<div style="clear:both"></div>
<br />

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<% if current_user.system_admin? %>
  <p>
    <b>Status:</b>
    <%= @user.status %>
  </p>
  
  <p>
    <b>System Admin:</b>
    <%= @user.system_admin? %>
  </p>
<% end %>
