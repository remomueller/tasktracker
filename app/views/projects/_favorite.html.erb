<%= link_to (@project.favorited_by?(current_user) ? 'Unfavorite' : 'Add to Favorites'.html_safe), favorite_project_path(@project, favorite: @project.favorited_by?(current_user) ? '0' : '1'), remote: true, method: :post, class: "btn #{ 'btn-primary' unless @project.favorited_by?(current_user)}" %>
